# Pretty URLs for competitor invite links (Apache / Railway heroku-php-apache2)
RewriteEngine On

# /competitor/{64-hex} → competitor.php?token=...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^competitor/([a-fA-F0-9]{64})/?$ competitor.php?token=$1 [L,QSA]
