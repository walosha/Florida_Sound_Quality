# Pretty URLs for competitor registration (Apache / Railway heroku-php-apache2)
RewriteEngine On

# /competitor → competitor.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^competitor/?$ competitor.php [L,QSA]
